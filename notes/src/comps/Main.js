import React from "react";

import NoteListings from "./NoteListings";

const Main = () => {
  return (
    <main style={{ margin: "2rem 0" }}>
      <NoteListings />
    </main>
  );
};

export default Main;
